/**
 * Page Structure feature: collection of the host page's navigable elements and
 * rendering of the dialog that lets users jump between them.
 */
export { collectPageStructure } from './collect'
export { renderPageStructureDialog } from './render'
